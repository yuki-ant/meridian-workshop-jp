GOAL  参加者が進め方を理解し、マシンの準備ができていて、「いまどこ？」ページを開いている。

SAY（自分の言葉で、短く）
- ようこそ。これから約90分、コンサルタントとして Meridian Components 社の RFP に応札し（第1幕）、受注した案件の最初の改修を納品します（第2幕）。私が一歩ずつ案内します。
- ターミナルで作業するステップと、ブラウザで短いページを読むステップがあります。一つ終わったら「できました」、または短く「Done」か「Next Step」と送ってください。いつ質問しても、途中でやめて後で戻ってきても大丈夫です。現在地は私が覚えています。
- スラッシュコマンド、Shift+Tab、許可プロンプト、`@` はあなたにしか操作できません。「入力してください」と言ったら、本当にあなたが入力してください。
- 何もインストールされていません。チューターはこのセッションの間だけ読み込まれています。

YOU DO
1. `node tutor/bin/tutor.mjs check` を実行する。git、Node、npm、uv、Python と、リポジトリの準備（タグ tutor-base）を確認する。
2. 足りないものがあれば、何が足りないかだけを伝える。uv は第2幕まで不要なので、第1幕はそのまま進めてよい（Node はチューター自体が使うので、動いている時点で入っている）。導入手順は第2幕の冒頭で案内する、と伝える。tutor-base がなければ `git tag tutor-base` を実行してよいか確認してから付ける。
3. `node tutor/bin/tutor.mjs status` を実行し、出力したマップを **全体マップ（表示のみ・入力不要）** の見出しの下にコードブロックで貼る。二幕で約81分、予備が約9分。
4. `node tutor/bin/tutor.mjs open map.html` を実行し、アドレスを「→ 開く:」の行で伝え、ワークショップの間ずっと開いておくよう頼む。何のページかを2行で説明する: 自動で更新される「いまどこ？」ページで、全体マップと現在のステップ、経過時間と90分の予算、いまやることのヒントが表示される。

DONE WHEN  check が通る（uv が第2幕までに入る見込みなら、参加者に確認して `node tutor/bin/tutor.mjs next --force` で進めてよい）。
