GOAL  作業ブランチを切り、Plan Mode で RFP の R1「概要ページで期間や場所を選んでも反映されない」の原因を調べ、直す計画に合意する。まだコードは編集しない。

SAY
- R1 に書かれているのは「反映されない」という症状だけです。まず原因を突き止め、どこまで直すかを決めてから手を動かします。
- Plan Mode では、Claude は読むだけで編集しません。調べて計画を立てる段階にぴったりです。

YOU DO
1. 作業ブランチを切ってよいか聞き、了承を得たら実行する: `git switch -c fix/overview-filters`
2. Plan Mode を案内する。Shift+Tab を2回押すと Plan Mode になり、あなたには発言の番が回らない。だから同じメッセージで、切り替わったら次を送るよう伝える。

[PASTE BLOCK]
RFP の R1 に着手します。「概要ページで期間や場所を選んでも反映されない」（主要指標が更新されない）という症状の原因を調べて、直す計画を立ててください。範囲は概要ページ（client/src/views/Dashboard.vue）だけです。

3. 調査は短く: フィルター → `useFilters` → `watch` → `loadData` → API はつながっていて、売上は変わる。原因は、上部の KPI のうち4つが固定値であること（`在庫回転率` と `平均処理時間` はテンプレートに直書き、`出荷完了` と `注文充足率` は固定の ref）。これを参加者に1〜2行で示す。
4. 計画は短く示す。KPI を、フィルター済みのデータ（`allOrders`）から計算する。定義は次を推奨し、参加者に確認する:
   - 出荷完了 = Delivered の件数。目標は全期間 200、単月は 17（200 ÷ 12 を丸め）
   - 注文充足率 = (Delivered + Shipped) ÷ 全注文 × 100
   - 平均処理時間 = Delivered の (actual_delivery − order_date) の平均日数。actual_delivery がないものは除く
   - 在庫回転率: **今回は計算しない。** 在庫金額に対して受注額が約70倍あり、どの定義でも目標 4.5 と比べられる値にならない。定義をクライアントに確認する事項として次フェーズに回し、それまでは直書きの 4.2 をやめて「—（定義を確認中）」と表示する。「データから出せないものを、もっともらしい数字で埋めない」のもコンサルタントの判断だ、と1行添える。
   - 目標との差（「-6.67%」などの直書き）も、計算した値から出す
   確認方法: 期間や場所を切り替えると、出荷完了・注文充足率・平均処理時間が変わる。データが0件のときに NaN や Infinity を出さない。
5. 参加者が計画を承認したら、Shift+Tab で Plan Mode を抜けてもらう。承認の返答例を PASTE BLOCK で添える。

RECORD  r1_scope=<合意した範囲と KPI の定義を一行で>

DONE WHEN  作業ブランチにいて、client/ と server/ はまだ未編集、範囲の合意が記録された。
