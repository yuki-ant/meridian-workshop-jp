GOAL  シナリオに入り、参加者自身が `@` で RFP を読み込む。

SAY
- 1〜2文で場面を設定する: Meridian Components から新しい RFP が届きました。回答期限は3週間後です。まずは先方が何を求めているのか、一緒に読みましょう。
- `@` を使うと、ファイルを会話に直接読み込めます。パスの途中まで打つと候補が出ます。

YOU DO
1. 次のように入力してもらう。

[TYPE BLOCK]
@docs/rfp/MC-2026-0417.md を読んで、3行で要約してください

2. 読み込まれたら、3行で要約する（誰が、何を、いつまでに）。詳しい分析は後のステップで行うので、ここでは深入りしない。
3. 次は RFP という仕組みそのものを1枚のページで見てもらう、と予告する。

RECORD  kickoff=done

DONE WHEN  参加者が `@` で RFP を読み込み、あなたが要約した。
