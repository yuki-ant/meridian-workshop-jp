GOAL  第2幕をここで締める。今日の成果と残りの作業を、クライアント向けの短い引き継ぎメモにまとめ、`proposal/next-phase.md` に書き、ブラウザで開く。

SAY
- 今日の納品はここまでです。現実の案件でも、一度に全部はやりません。何が終わって、何が次なのかをはっきりさせて渡すのが、コンサルタントの仕事です。

YOU DO
1. `proposal/next-phase.md` を書く（1ページ以内）:
   - 今回の成果: 概要ページ（発注ボタンの日本語化、/api/tasks の実装）と Reports のフィルター接続、レビュー指摘の修正。変えたファイルとコミットのハッシュ。
   - 次フェーズ: 「Create PO」の発注書機能（モーダルと /api/purchase-orders）、R1 の残り（金額表示、i18n の文言、console のノイズ、Composition API への移行、2025年決め打ちの四半期判定など）、レビューで次フェーズに回した指摘。実際のコードで確かめた項目だけを書く。
   - その後: R4 アーキテクチャ文書、R3 E2E テスト（Playwright MCP が `.mcp.json` に設定済み）、R2 発注推奨ビュー（着手前に決めること3点: 予算内での優先順位、部分発注の可否、需要予測がない品目の扱い）
   - 提案書（proposal/proposal.md、または proposal/ の .md ファイル）のスケジュールとの対応
2. `node tutor/bin/tutor.mjs open proposal/next-phase.md` を実行し（ページサーバーが Markdown を読みやすいページに整形して表示する）、T が出力したアドレスを「→ 開く:」の行で伝える。「いまどこ？」ページの「あなたの成果物」からも開ける、と一言添える。
3. 参加者の確認や修正の依頼は待たない。レポートを開いたら、同じターンのうちに `node tutor/bin/tutor.mjs next` を実行し、出力された締めの台本に従う。「一読して直したい点を教えてください」のような問いかけはしない。

DONE WHEN  proposal/next-phase.md がある。これが最後のステップで、書き上げて開いた時点でワークショップは完了。
