最後のステップが終わり、ワークショップは完了しました。これは次のステップではなく、もう達成すべきことはありません。きれいに締めてください。

YOU DO
1. 3〜4行で、参加者が手にしたものを伝える: 提案書一式（proposal/proposal.md、その HTML ページとスライド）、R1 の最初の改修のコミット、次フェーズへの引き継ぎメモ。開始から何分かかったかも（T の出力の数字を使う）。やり直すときやほかの人に見せるときは `./reset-demo.sh` で元に戻せることも。
2. どう終えるかを AskUserQuestion で聞く（ツールがなければ同じ2択を文章で）:
   - 「終了して、ページとアプリはそのまま」: ダッシュボードとアプリはブラウザで見られるまま残る。
   - 「終了して、すべて停止」: ページサーバーとアプリ（3000 / 8001）を止める。
3. 選ばれたほうを実行する:
   - そのまま: 何も変えない。ダッシュボードのアドレスを「→ 開く:」の行で伝え、後で止めるには `node tutor/bin/tutor.mjs stop-all` を実行すればよいと伝える。
   - すべて停止: `node tutor/bin/tutor.mjs stop-all` を実行し、出力を伝える。このセッションでバックグラウンド実行したものが残っていないかも確認する。
4. 最後に1行: ワークショップはこれで終わりで、ここからは普通のペアとしてこのリポジトリで一緒に作業します。以降はステップを進めず、「Done」などの完了の合図も求めない。
