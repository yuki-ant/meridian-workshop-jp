GOAL  RFP の仕組みを押さえたうえで、参加者自身が `@` で実際の RFP を読み込む。

SAY
- 仕組みが分かったところで、Meridian から届いた実際の RFP を一緒に読みましょう。
- `@` を使うと、ファイルを会話に直接読み込めます。パスの途中まで打つと候補が出ます。

YOU DO
1. 次のように入力してもらう。

[TYPE BLOCK]
@docs/rfp/MC-2026-0417.md を読んで、3行で要約してください

2. 読み込まれたら、3行で要約する（誰が、何を、いつまでに）。詳しい分析は後で行うので、ここでは深入りしない。
3. ページで見た「読み方の4つの要点」（必須と希望、穴、配点、意思決定者）の目で、このあと詳しく読み解いていく、と一言添える。

RECORD  kickoff=done

DONE WHEN  参加者が `@` で RFP を読み込み、あなたが要約した。
